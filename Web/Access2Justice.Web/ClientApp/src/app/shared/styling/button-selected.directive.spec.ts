import { ButtonSelectedDirective } from './button-selected.directive';

describe('ButtonSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
